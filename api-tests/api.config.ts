export const Api = {
  // Update these after observing network calls in the app (DevTools → Network).
  // Keep them as black-box “contract” endpoints, not internal assumptions.
  me: "/api/me",
  // Example placeholders:
  // upload: "/api/datasets/upload",
  // pipelineStatus: (id: string) => `/api/pipelines/${id}/status`,
  // download: (id: string) => `/api/exports/${id}/download`,
};
