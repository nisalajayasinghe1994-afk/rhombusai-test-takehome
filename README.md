# Rhombus AI – Software Engineer (Test) Take-Home

This repo contains:
- Test strategy (`test-strategy.md`)
- UI automation (Playwright) in `/ui-tests`
- API/network tests (Playwright request) in `/api-tests`
- Data validation script (Python) in `/data-validation`
- CI design (`ci-design.yml`)

## Prerequisites
- Node 20+
- Python 3.10+
- A Rhombus AI account (free plan is fine)

## Setup
1) Install dependencies:
```bash
npm ci
npm run pw:install
